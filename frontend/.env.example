# API Base URL (optional - defaults to '' for same-origin requests via Vite proxy)
# Set this to your backend URL in production
VITE_API_URL=

# For local development without proxy:
# VITE_API_URL=http://localhost:8080
